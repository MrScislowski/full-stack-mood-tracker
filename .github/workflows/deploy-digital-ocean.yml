name: Deploy to DigitalOcean

on:
  workflow_dispatch: {}
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SERVER_SSH_KEY }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}

      - name: Download code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: install dependencies
        run: pnpm install

      - name: run linter
        run: pnpm run lint

      - name: build
        run: pnpm run build

      - name: copy files over to Digital Ocean
        run: |
          scp package.json nodeapp@159.223.191.151:~/full-stack-mood-tracker

      # - name: Deploy to DigitalOcean
      #   run: |
      #     ssh nodeapp@159.223.191.151 /bin/bash << EOF
      #     cd ~/full-stack-mood-tracker
      #     pnpm install
      #     pnpm build
      #     pm2 restart ./backend/dist/backend/index.js
      #     EOF
